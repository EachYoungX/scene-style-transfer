# V1.5.3 固定 Purification 运行结果

## 运行状态

冻结配置已写入：

`configs/experiment/v1.5.2_frozen_purification.yaml`

新增运行已完成：

- seed 123：3 个配对 × 2 个固定基线；
- seed 777：3 个配对 × 2 个固定基线；
- 共 12 个新增输出；
- 未重新扫描倍率、rank 或时间表。

固定基线：

- `P_MASK_HIGHNORM_X1`：`mask_highnorm`，A2 highres path，倍率 1.0；
- `P_SVD_FIXED_X1`：`svd_fixed`，A2 highres path，倍率 1.0。

## 自动校验

- 12/12 输出图存在；
- 12/12 residual 日志存在；
- 每个日志包含 22 个有效 img2img denoising step；
- step 范围为 0--21；
- timestep 无 `None`；
- 使用中性 manifest prompt，没有追加风格提示；
- 环境为 `diffusers 0.35.2`、`transformers 4.57.1`、`torch 2.5.1`。

运行目录：

- `runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_multiseed_seed123/`
- `runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_multiseed_seed777/`

校验文件：

`runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_summary/runtime_verification.json`

## 人工查看入口

固定顺序对比图位于：

`runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_comparison/`

每张图的列顺序固定为：

```text
content | reference | matched A0 | A2 | P_MASK_HIGHNORM_X1 | P_SVD_FIXED_X1
```

每个配对有 seed 42、123、777 三张图：

- `demuth_church_seed42.png`
- `demuth_church_seed123.png`
- `demuth_church_seed777.png`
- `demuth_wave_seed42.png`
- `demuth_wave_seed123.png`
- `demuth_wave_seed777.png`
- `kulhanek_snow_winter_seed42.png`
- `kulhanek_snow_winter_seed123.png`
- `kulhanek_snow_winter_seed777.png`

## 人工评分

评分表：

`runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_comparison/blind_review_sheet.csv`

盲化对比图位于：

`runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_comparison/blind_grids/`

图中只显示 `content`、`reference` 和 `anon_1`--`anon_4`，方法列已按每个 `case + seed` 随机匿名化。解盲文件为：

`runs/ip_adapter_plus_injection/v1_5_2_frozen_purification_comparison/blind_key.json`

每个匿名方法评分 0--4，数字表达同组内相对差异，不是跨样本绝对质量分：

- `content_identity`：越高越好；
- `structure_preservation`：越高越好；
- `style_strength`：越高越好；
- `reference_leakage`：越高表示泄漏越严重；
- `local_geometry_risk`：越高表示几何风险越严重。

同时在 `notes` 中记录以下二值失败标签及区域：

- `new_reference_object`；
- `false_hard_edge`；
- `geometry_takeover`；
- `scene_regeneration`。

## 当前不能提前下的结论

在人工评分完成前，不能判断 purification 是否满足 `Pause` 条件，也不能正式决定是否进入 Geometry-Gated HighRes Boost。seed 42 的探索结果只能作为背景，不能替代本轮三 seed 固定配置的组内比较。

Residual energy 只表示 conditioning magnitude，不表示实际输出风格质量；Edge F1 或其他自动结构指标也不能单独代表几何安全。

人工评分完成后的结论见：[v1_5.3人工评审结论.md](./v1_5.3人工评审结论.md)。
